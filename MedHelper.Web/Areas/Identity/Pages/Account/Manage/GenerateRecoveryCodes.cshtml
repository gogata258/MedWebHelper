@page
@model GenerateRecoveryCodesModel
@{
	ViewData["Title"] = "Generate two-factor authentication (2FA) recovery codes";
	ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<tag_partial name="_StatusMessagePartial" model="@Model.StatusMessage" />
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
	<p><span class="fa fa-exclamation-circle fa-2x pr-2"></span><strong>Put these codes in a safe place.</strong></p>
	<p>If you lose your device and don't have the recovery codes you will lose access to your account.</p>
	<p>Generating new recovery codes does not change the keys used in authenticator apps. If you wish to change the key used in an authenticator app you should <tag_a asp-page="./ResetAuthenticator">reset your authenticator keys.</tag_a></p>
</div>
<div>
	<tag_form method="post" class="form-group">
		<input class="btn btn-danger" type="submit" value="Generate Recovery Codes" />
	</tag_form>
</div>